@namespace SMS.Components.Layout
@inject IJSRuntime JSRuntime
@inject NavigationManager NavigationManager
@rendermode InteractiveServer
@implements IDisposable

@if (!string.IsNullOrEmpty(currentUser))
{
    <div style="display: flex; align-items: center; gap: 16px;">
        <!-- 实时时间显示 -->
        <AntDesign.Tag style="background: rgba(255, 255, 255, 0.1); border: none; color: white; border-radius: 15px; padding: 6px 12px;">
            @currentTime
        </AntDesign.Tag>
        
        <!-- 用户菜单 -->
        <AntDesign.Dropdown>
            <Overlay>
                <AntDesign.Menu>
                    <AntDesign.MenuItem Key="password" Icon="key">
                        <a href="/forgot-password" style="text-decoration: none; color: inherit;">修改密码</a>
                    </AntDesign.MenuItem>
                    <AntDesign.MenuDivider />
                    <AntDesign.MenuItem Key="logout" Icon="logout" OnClick="Logout">
                        <span style="color: #ff4d4f;">退出登录</span>
                    </AntDesign.MenuItem>
                </AntDesign.Menu>
            </Overlay>
            <ChildContent>
                <AntDesign.Button Ghost="true" Icon="user" style="border-radius: 20px; color: white; border-color: white;">
                    @currentUser
                </AntDesign.Button>
            </ChildContent>
        </AntDesign.Dropdown>
    </div>
}
else
{
    <a href="/login" style="text-decoration: none;">
        <AntDesign.Button Ghost="true" style="border-radius: 20px; color: white; border-color: white;">
            登录
        </AntDesign.Button>
    </a>
}

@code {
    private string currentUser = string.Empty;
    private string currentTime = string.Empty;
    private System.Threading.Timer? timer;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await LoadCurrentUser();
            UpdateTime();
            StartTimer();
            StateHasChanged();
        }
    }

    private void StartTimer()
    {
        timer = new System.Threading.Timer(async _ =>
        {
            UpdateTime();
            await InvokeAsync(StateHasChanged);
        }, null, TimeSpan.Zero, TimeSpan.FromSeconds(1));
    }

    private void UpdateTime()
    {
        currentTime = DateTime.Now.ToString("yyyy/MM/dd HH:mm:ss");
    }

    private async Task LoadCurrentUser()
    {
        try
        {
            // 从浏览器存储获取用户名
            currentUser = await JSRuntime.InvokeAsync<string>("localStorage.getItem", "currentUser") ?? string.Empty;
        }
        catch
        {
            currentUser = string.Empty;
        }
    }

    private async Task Logout()
    {
        try
        {
            // 清除本地存储的用户信息
            await JSRuntime.InvokeVoidAsync("localStorage.removeItem", "currentUser");
            
            // 显示退出提示
            await JSRuntime.InvokeVoidAsync("alert", "已成功退出登录");
            
            // 跳转到登录页面
            NavigationManager.NavigateTo("/login", forceLoad: true);
        }
        catch (Exception ex)
        {
            await JSRuntime.InvokeVoidAsync("alert", $"退出登录失败: {ex.Message}");
        }
    }

    public void Dispose()
    {
        timer?.Dispose();
    }
}
